# This experiment needs no external secrets or environment variables.
# Generation + build require only `go`. Wire-parity verification needs
# a kind cluster (aggexp-0046) and a serving cert (hack/gen-certs.sh),
# neither of which is a secret. This file is intentionally empty of
# real values and kept only for the standard experiment layout.
