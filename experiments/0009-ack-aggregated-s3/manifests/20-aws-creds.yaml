# AWS credentials. The values are placeholders the mock ignores; the
# SDK only requires they be non-empty to sign requests.
apiVersion: v1
kind: Secret
metadata:
  name: aws-creds
  namespace: aggexp-system
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "test"
  AWS_SECRET_ACCESS_KEY: "test"
